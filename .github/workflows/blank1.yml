name: Another Workflow

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  check:
    permissions:
      pull-requests: write
    name: Version Calculation
    uses: ./.github/workflows/blank.yml
