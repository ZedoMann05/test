name: Check File Sizes

on:
  push:
    branches:
      - main

jobs:
  check_file_sizes:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run file size check script
      run: |
        chmod +x check.sh
        OUTPUT=$(./check.sh)
        if [[ $OUTPUT =~ "Error: File" ]]; then
          echo "$OUTPUT"
          exit 1
        fi
        echo -e "\e[32mAll assets match required size\e[0m"
