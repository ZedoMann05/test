name: Check commit

on:
  workflow_call:

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
        
    - name: Validate Commits
      run: |
        commits=$(curl -s "https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/commits" | jq -r '.[].sha')
        
        branch_pattern=$(echo ${{ github.event.pull_request.head.ref }} | grep -oE '\/([a-zA-Z0-9_-]+)\/' | cut -d'/' -f2)

        valid_commits=true
        for commit_sha in $commits; do
          if ! git log --format=%B -n 1 $commit_sha | grep -E "^(fix|hotfix|bugfix|feat|feature|breaking)\($branch_pattern\)?:.+"; then
            valid_commits=false
            break
          fi
        done

        if $valid_commits; then
          echo "# Validate Commit" > ./report.md
          echo "Your commits:" >> ./report.md
          for commit_sha in $commits; do
            echo "- $(git log --format=%B -n 1 $commit_sha)" >> ./report.md
          done
          echo "🟢 All new commits are valid according to the regulation." >> ./report.md
          exit 0
        else
          echo "# Validate Commit" > ./report.md
          echo "Your commits:" >> ./report.md
          for commit_sha in $commits; do
            echo "- $(git log --format=%B -n 1 $commit_sha)" >> ./report.md
          done
          echo "🔴 Not all new commits are valid according to the regulation." >> ./report.md
          exit 1
        fi
    
    - name: Post comment on Pull Request
      if: ${{ always() }}
      uses: thollander/actions-comment-pull-request@v2
      with:
        filePath: ./report.md
