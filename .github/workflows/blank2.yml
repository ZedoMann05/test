name: Check commit

on:
  workflow_call:

jobs:
  check_commit:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: check commit
      run: |
        last_commit_message=$(git show -s --format="%s" ${{ github.sha }})
        if ! echo "$last_commit_message" | grep -qE "(fix|hotfix|bugfix|feat|feature|breaking)((.+))?:.+"; then
          echo "::set-output name=validation_result::invalid"
        else
          echo "::set-output name=validation_result::valid"
        fi
    
    - name: Post report as comment
      if: ${{ always() }}
      uses: thollander/actions-comment-pull-request@v2
      with:
        message: |
          const result = process.env.VALID_COMMITS_RESULT;
          const message = result === 'valid' ? '🟢 There are valid commits according to regulation.' : '🔴 There is no commits that are valid according to regulation';
          const octokit = github.getOctokit(process.env.GITHUB_TOKEN);
          await octokit.issues.createComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
            body: message
          });